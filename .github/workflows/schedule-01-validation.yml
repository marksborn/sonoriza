name: SCHEDULE-01 validation

on:
  push:
    branches:
      - agent/schedule-01-recurring-target-policies-58
    paths:
      - .github/workflows/schedule-01-validation.yml
      - .github/schedule58-stage1.py
      - .github/schedule58-stage2.py
      - .github/schedule58-stage3.py
      - .github/schedule58-stage4.py
      - .github/schedule58-stage5.py

permissions:
  contents: write

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 35
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: sonoriza
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d sonoriza"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 12

    env:
      DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/sonoriza
      AUTH_SECRET: schedule-01-validation-secret
      AUTH_SPOTIFY_ID: test-spotify-id
      AUTH_SPOTIFY_SECRET: test-spotify-secret
      AUTH_GOOGLE_ID: test-google-id
      AUTH_GOOGLE_SECRET: test-google-secret
      CRON_SECRET: test-cron-secret
      SONORIZA_ALLOWED_EMAILS: test@example.com

    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: agent/schedule-01-recurring-target-policies-58
          fetch-depth: 0

      - name: Setup Node 24.19.0
        uses: actions/setup-node@v4
        with:
          node-version: 24.19.0
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Apply SCHEDULE-01 staged patches
        shell: bash
        run: |
          apply_patch() {
            local file="$1"
            [ -f "$file" ] || return 0
            local output
            if ! output="$(python "$file" 2>&1)"; then
              output="$(printf '%s' "$output" | tail -c 9000)"
              output="${output//$'\n'/'%0A'}"
              echo "::error title=SCHEDULE-01 patch failed::$output"
              exit 1
            fi
            echo "$output"
          }
          apply_patch .github/schedule58-stage1.py
          apply_patch .github/schedule58-stage2.py
          apply_patch .github/schedule58-stage3.py
          apply_patch .github/schedule58-stage4.py
          apply_patch .github/schedule58-stage5.py

      - name: Format and generate Prisma
        run: |
          npx prisma format
          npx prisma validate
          npx prisma generate

      - name: Apply migrations on disposable PostgreSQL
        run: npx prisma migrate deploy

      - name: Full regression gate
        shell: bash
        run: |
          run_checked() {
            local title="$1"
            shift
            local output
            if ! output="$("$@" 2>&1)"; then
              output="$(printf '%s' "$output" | tail -c 9000)"
              output="${output//$'\n'/'%0A'}"
              echo "::error title=$title::$output"
              exit 1
            fi
            echo "$output"
          }
          run_checked "SCHEDULE-01 core" npx tsx --test src/services/target-schedule.test.ts src/services/keep-filled-core.test.ts
          run_checked "Planner regressions" npm run test:planner
          run_checked "Spotify regressions" npm run test:spotify
          run_checked "Incremental regressions" npm run test:incremental
          run_checked "Simulation regressions" npm run test:simulation
          run_checked "Typecheck" npm run typecheck
          run_checked "Production build" npm run build

      - name: Commit validated product changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add prisma src docs || true
          if git diff --cached --quiet; then
            echo "No product changes to commit"
          else
            git diff --cached --stat
            git commit -m "feat: SCHEDULE-01 — recurring target policies (#58)"
            git push origin HEAD:agent/schedule-01-recurring-target-policies-58
          fi
